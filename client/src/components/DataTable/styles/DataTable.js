import {makeStyles} from "@material-ui/styles";

export default makeStyles({
    table: {
        minWidth: 650,
        maxHeight:'50rem',
        overflowY:'scroll',
        
    },
    headers:{
        fontWeight:'bold',
    },
    actions:{
        // display:'flex',
    }
})